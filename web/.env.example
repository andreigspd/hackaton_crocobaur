# Where the frontend sends API calls.
# In dev, leave as /api and Vite proxies to the backend (see vite.config.js).
# In production, set this to your deployed backend URL, e.g. https://onepick-api.onrender.com
VITE_API_URL=http://localhost:8000
